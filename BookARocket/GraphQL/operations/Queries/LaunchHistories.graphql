query LaunchHistories {
  launches {
    cursor
    hasMore
    launches {
      id
      site
      mission {
        name
        missionPatch
      }
      rocket {
        id
        name
        type
      }
    }
  }
}
