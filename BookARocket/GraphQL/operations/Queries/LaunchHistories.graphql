query LaunchHistories {
  launches {
    cursor
    hasMore
    launches {
      id
      site
      mission {
        name
        missionPatch(size: SMALL)
      }
      rocket {
        id
        name
        type
      }
    }
  }
}
