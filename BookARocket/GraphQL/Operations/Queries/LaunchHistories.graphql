query LaunchHistories($pageSize: Int) {
  launches(pageSize: $pageSize) {
    cursor
    hasMore
    launches {
      id
      site
      mission {
        name
        missionPatch
      }
      rocket {
        id
        name
        type
      }
    }
  }
}
